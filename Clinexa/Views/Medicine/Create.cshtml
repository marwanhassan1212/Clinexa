@model Clinexa.Models.ViewModels.Medicine.MedicineCreateViewModel

@{
    ViewData["Title"] = "Add Medicine";
}

@section PageActions {
    <a asp-action="Index"
       class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left me-1"></i>
        Back to Medicines
    </a>
}

<div class="row justify-content-center">

    <div class="col-lg-8 col-xl-7">

        <div class="card">

            <div class="card-header">
                <div>
                    <h3 class="card-title mb-1">
                        Add Medicine
                    </h3>

                    <div class="text-secondary">
                        Add a medicine to the Clinexa medication catalog.
                    </div>
                </div>
            </div>

            <form asp-action="Create" method="post">

                @Html.AntiForgeryToken()

                <div class="card-body">

                    <div asp-validation-summary="ModelOnly"
                         class="text-danger mb-3">
                    </div>

                    <!-- Medicine Name -->
                    <div class="mb-3">

                        <label asp-for="Name"
                               class="form-label">
                            Medicine Name
                        </label>

                        <input asp-for="Name"
                               class="form-control"
                               placeholder="e.g. Panadol" />

                        <span asp-validation-for="Name"
                              class="text-danger">
                        </span>

                    </div>

                    <!-- Generic Name -->
                    <div class="mb-3">

                        <label asp-for="GenericName"
                               class="form-label">
                            Generic Name
                        </label>

                        <input asp-for="GenericName"
                               class="form-control"
                               placeholder="e.g. Paracetamol" />

                        <span class="form-hint">
                            The active ingredient or scientific name.
                        </span>

                        <span asp-validation-for="GenericName"
                              class="text-danger">
                        </span>

                    </div>

                    <!-- Description -->
                    <div class="mb-0">

                        <label asp-for="Description"
                               class="form-label">
                            Description
                        </label>

                        <textarea asp-for="Description"
                                  class="form-control"
                                  rows="5"
                                  placeholder="Add any useful information about this medicine..."></textarea>

                        <span asp-validation-for="Description"
                              class="text-danger">
                        </span>

                    </div>

                </div>

                <div class="card-footer d-flex justify-content-end gap-2">

                    <a asp-action="Index"
                       class="btn btn-outline-secondary">
                        Cancel
                    </a>

                    <button type="submit"
                            class="btn btn-primary">

                        <i class="bi bi-check-lg me-1"></i>
                        Save Medicine

                    </button>

                </div>

            </form>

        </div>

    </div>

</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}